<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itShuai.mapper.AdminMapper">

    <select id="LoginAsAdmin" resultType="com.itShuai.pojo.Admin">
        select * from couriersystem.admin where Account=#{Account} and Password=#{Password};
    </select>
    <select id="FindAllDelivery" resultType="com.itShuai.pojo.Delivery">
        select * from couriersystem.delivery;
    </select>
    <select id="selectDeliveryByDeliveryID" resultType="com.itShuai.pojo.Delivery">
        select * from couriersystem.delivery,user where delivery.DeliveryId=#{DeliveryId} and user.UserId = delivery.RecipientId;
    </select>
    <update id="ChangeDeliveryStatus">
        update couriersystem.deliverystatus set DeliveryStatus=#{DeliveryStatus} where DeliveryId=#{DeliveryId};
    </update>
</mapper>
